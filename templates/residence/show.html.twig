{% extends 'base.html.twig' %}

{% block title %}Voir une adresse{% endblock %}

{% block content %}
    <h1>Adresses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ location.id }}</td>
            </tr>
            <tr>
                <th>Rue</th>
                <td>{{ location.street }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ location.city }}</td>
            </tr>
            <tr>
                <th>Code Postal</th>
                <td>{{ location.zipCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('location_index') }}">Retour à la liste</a>

    <a href="{{ path('location_edit', {'id': location.id}) }}">Modifier</a>

    {{ include('residence/_delete_form.html.twig') }}
{% endblock %}